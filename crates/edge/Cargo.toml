[package]
name = "edge"
version = "0.1.0"
edition = "2024"

[dependencies]
argus-core = { path = "../core" }
tokio = { version = "1.0", features = ["full"] }
axum = "0.8.8"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
